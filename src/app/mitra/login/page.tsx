import { AuthForm } from "@/components/account/auth-forms";

export const metadata = { title: "Masuk Mitra | AFA MITRA" };

export default function MitraLoginPage() {
    return <AuthForm mode="login" />;
}
