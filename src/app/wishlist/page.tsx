"use client";

import Image from "next/image";
import Link from "next/link";
import { AnimatePresence, motion } from "framer-motion";
import { Heart, ShoppingCart, Trash2 } from "lucide-react";
import { useCart } from "@/context/cart-context";
import { useWishlist } from "@/context/wishlist-context";
import { formatRupiah } from "@/lib/data";

function WishlistToast({ message }: { message: string }) {
    return <AnimatePresence>{message && <motion.div initial={{ opacity: 0, y: -18, scale: 0.96 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: -18, scale: 0.96 }} className="fixed left-1/2 top-5 z-100 w-[calc(100%-2rem)] max-w-sm -translate-x-1/2 rounded-full border border-[#C8A45D]/30 bg-white px-5 py-3 text-center font-semibold text-[#2E2A26] shadow-[0_18px_45px_rgba(46,42,38,0.16)]">{message}</motion.div>}</AnimatePresence>;
}

export default function WishlistPage() {
    const { addToCart } = useCart();
    const { wishlist, removeFromWishlist, toast } = useWishlist();

    return <main className="min-h-screen bg-[radial-gradient(circle_at_12%_10%,rgba(200,164,93,0.22),transparent_30%),linear-gradient(135deg,#F8F4EC_0%,#FFFDF8_55%,#EFE6D5_100%)] px-4 py-8 text-[#2E2A26]"><WishlistToast message={toast} /><div className="mx-auto max-w-7xl"><header className="mb-8 flex flex-col gap-4 rounded-4xl border border-[#C8A45D]/20 bg-white/70 p-6 shadow-[0_24px_70px_rgba(46,42,38,0.1)] backdrop-blur md:flex-row md:items-center md:justify-between"><div><Link href="/" className="text-sm font-semibold text-[#8B6B3F] hover:text-[#C8A45D]">← Kembali ke AFA Store</Link><h1 className="mt-3 font-display text-5xl font-bold text-[#2E2A26] md:text-6xl">Wishlist</h1><p className="mt-2 max-w-2xl text-[#8B6B3F]">Produk favorit Anda tersimpan rapi dan siap dimasukkan ke keranjang kapan saja.</p></div><div className="inline-flex w-fit items-center gap-2 rounded-full bg-[#C8A45D]/10 px-5 py-3 font-bold text-[#8B6B3F]"><Heart className="text-red-500" fill="#ef4444" /> {wishlist.length} Item</div></header>{wishlist.length === 0 ? <motion.section initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} className="grid min-h-[52vh] place-items-center rounded-4xl border border-[#C8A45D]/20 bg-white/80 p-8 text-center shadow-[0_24px_70px_rgba(46,42,38,0.1)]"><div><div className="text-6xl">❤️</div><h2 className="mt-5 font-display text-4xl font-bold">Wishlist masih kosong</h2><p className="mx-auto mt-2 max-w-md text-[#8B6B3F]">Pilih produk premium AFA Store yang Anda suka, lalu simpan dengan tombol hati.</p><Link href="/#bawang-goreng" className="mt-6 inline-flex rounded-full bg-[#C8A45D] px-7 py-3 font-bold text-white shadow-[0_18px_40px_rgba(200,164,93,0.28)] transition hover:bg-[#A9853F]">Lihat Produk</Link></div></motion.section> : <section className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{wishlist.map((item) => <motion.article key={item.id} initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} whileHover={{ y: -6, scale: 1.01 }} className="overflow-hidden rounded-3xl border border-[#C8A45D]/15 bg-white p-4 shadow-[0_18px_45px_rgba(46,42,38,0.09)]"><div className="relative h-56 overflow-hidden rounded-2xl bg-linear-to-br from-[#FFF8EA] via-white to-[#EFE6D5]"><Image src={item.image} alt={item.name} fill sizes="(min-width: 1280px) 25vw, (min-width: 768px) 33vw, 100vw" className="object-contain p-4" /></div><h2 className="mt-5 font-display text-2xl font-bold">{item.name}</h2><p className="mt-1 text-xl font-bold text-[#8B6B3F]">{formatRupiah(item.price)}</p><div className="mt-5 grid gap-2"><motion.button whileHover={{ scale: 1.03 }} whileTap={{ scale: 0.98 }} onClick={() => addToCart(item)} className="rounded-full bg-[#C8A45D] px-5 py-3 font-bold text-white shadow-[0_14px_30px_rgba(200,164,93,0.22)] transition hover:bg-[#A9853F]"><ShoppingCart className="mr-2 inline" size={18} />Masukkan Keranjang</motion.button><motion.button whileHover={{ scale: 1.03 }} whileTap={{ scale: 0.98 }} onClick={() => removeFromWishlist(item.id)} className="rounded-full border border-red-200 bg-white px-5 py-3 font-bold text-red-500 transition hover:bg-red-50"><Trash2 className="mr-2 inline" size={18} />Hapus</motion.button></div></motion.article>)}</section>}</div></main>;
}