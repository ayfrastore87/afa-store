"use client";
import { useState } from "react";
import { supabase } from "@/lib/supabase";
import { useRouter } from "next/navigation";
export default function KasirLoginForm() {
    const router = useRouter(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [remember,setRemember]=useState(false); const [error,setError]=useState(""); const [loading,setLoading]=useState(false);
    async function submit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(""); const { data, error: authError } = await supabase.auth.signInWithPassword({ email: email.trim(), password }); if (authError || !data.user) { setError("Email atau password salah."); setLoading(false); return; } const { data: account } = await supabase.from("users").select("role,isActive").eq("auth_id", data.user.id).single(); if (!account || account.isActive === false || !["admin","cashier"].includes(account.role)) { await supabase.auth.signOut(); setError("Akun ini tidak memiliki akses Kasir."); setLoading(false); return; } void remember; router.replace("/kasir"); router.refresh(); }
    return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-bold">Email Kasir<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-[#123524]/15 bg-white px-4" /></label><label className="block text-sm font-bold">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-[#123524]/15 bg-white px-4" /></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)} /> Ingat Saya</label>{error&&<p className="text-sm font-semibold text-red-600">{error}</p>}<button disabled={loading} className="h-12 w-full rounded-xl bg-[#184D47] font-bold text-[#F8F5EE]">{loading?"Memproses...":"MASUK"}</button><a href="/forgot-password" className="block text-center text-sm text-[#184D47] underline">Lupa Password?</a></form>;
}